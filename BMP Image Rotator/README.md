# BMP Image Rotator

Takes a .bmp file as argument and outputs out.bmp as a clone of the initial bmp file but rotated by 90 degrees

### Building the project
Standard version
```bash
make || make prod
```
Debug version / with debug flags
```bash
make debug
```
Cleaning project executables
```bash
make clean
```
