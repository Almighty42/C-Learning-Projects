# Hexdump

A clone of a unix utility hexdump ( more precisely - hexdump --canonical option ) - takes a file, turns it into a byte stream and then shows that byte stream in hex.

### Building the project
Standard version
```bash
make hexdump
```
Debug version / with debug flags
```bash
make hexdump_debug
```
